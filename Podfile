# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'Persession' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Persession
  pod 'RealmSwift'
  pod 'Dip'
  target 'PersessionTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PersessionUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
